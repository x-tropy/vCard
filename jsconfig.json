// Locate root folder with `@/` for imports
// even if the file is nested in a subfolder

{
  "compilerOptions": {
    // Module Aliases
    "baseUrl": ".",
    "paths": {
      "@/*": ["./*"]
    }
  }
}
